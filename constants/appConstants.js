const APP_CONSTANTS = {
	DEFAULT_ROLE: "user",
	SUPPORTED_LANGUAGES: ["en"],
	CLIENT_CODES: ["readers-realm"], // List of valid client codes
	AUTH_TOKEN_TYPE: 'O-Bearer',
	SUCCESS: 'success',
    FAIL: 'fail'
};

module.exports = APP_CONSTANTS;